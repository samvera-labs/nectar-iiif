import { MetadataItem } from "@iiif/presentation-3";
import React from "react";

interface Props {
  metadata: MetadataItem[];
}

const App: React.FC<Props> = ({ metadata }) => <div></div>;

export default App;
